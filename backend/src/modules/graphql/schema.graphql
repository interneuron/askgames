type Topic {
    id: Int!
    gameId: Int
    title: String
    text: String
    game: Game
    answers: [TopicAnswer]
    author: Account
}

type Game {
    id: Int!
    name: String
}

type TopicAnswer {
    id: Int!
    text: String
    rate: Int
    author: Account
}

type Account {
    id: Int!
    name: String
}

type Query {
    topic(id: Int!): Topic
    latestTopics: [Topic]
}
